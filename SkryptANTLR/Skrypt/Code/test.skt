import Impro

img = Image(256,256)

print("Rendering image...")

for (y = 0, y < img.Height, y++) {
	for (x = 0, x < img.Width, x++) {
		img.SetPixel(x,y,Color(Math.Random * 255))
	}
}

WriteImage(img, "test.png")

print("Image saved!")


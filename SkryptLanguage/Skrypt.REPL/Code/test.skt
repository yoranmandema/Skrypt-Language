
struct BasicStruct {
    A = 0
    B = ""

    fn init (a,b) {
        self.A = a
        self.B = b
    }

    fn toString () {
        return "{" + self.A + "," + self.B + "}"
    }
}

instance = BasicStruct(1,"Hello")

toString = "" + instance

print(toString)